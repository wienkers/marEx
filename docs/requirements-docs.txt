# Documentation build dependencies for marEx.
#
# The scientific stack (numpy, scipy, xarray, dask, ...) is installed separately
# via `pip install .[full]` in .readthedocs.yaml, so it is intentionally NOT
# duplicated here. This file lists only the tools needed to *build* the docs.

# Core Sphinx + theme
sphinx>=7.0
pydata-sphinx-theme>=0.15

# Sphinx extensions
sphinx-copybutton>=0.5.0
sphinx-design>=0.5.0
myst-parser>=2.0
nbsphinx>=0.9.0
sphinxcontrib-video>=0.2.0
sphinx-reredirects>=0.1.3

# Notebook rendering (nbsphinx requires pandoc + a kernel)
ipykernel
